"""
This package defines the FastAPI application and its components.

The application is structured to keep configuration, database models,
authentication utilities, schemas, and API routers in separate modules.
This improves modularity and makes the code easier to maintain.
"""

# Expose commonly used names at the package level if desired
